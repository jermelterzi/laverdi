class Recipe {
  Recipe({
    required this.name,
    required this.time,
    required this.difficulty,
    required this.yield,
  });

  final String name;
  final int time;
  final String difficulty;
  final int yield;
}
