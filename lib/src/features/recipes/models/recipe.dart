class Recipe {
  final String name;
  final int time;
  final String difficulty;
  final int yield;

  Recipe({
    required this.name,
    required this.time,
    required this.difficulty,
    required this.yield,
  });
}
